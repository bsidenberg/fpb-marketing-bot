# PRIME — BUILD REPORT, 2026-07-31

Session contract: `harness/BUILD-QUEUE-2026-08-V2.md`. Executed per its explicit authorization,
this repo's `CLAUDE.md`, and `harness/HARNESS.md`. This report follows the queue's own §8 format.

## Headline

**S-08A.1a.2a (discharge D-11) is ACCEPTED.** S-AUTOLOG-1 (fix the flight recorder) is
**BUILT, NOT ACCEPTED** — a cold safety review found a real production-behavior risk in
deploying the fix as-is, and the queue's own stopping rule (§7: "a cold safety-reviewer
returns BLOCK on a money-path session") applies. **The queue stopped there.** S-SNAPSHOT-1
was not attempted.

**Nothing was committed, pushed, or deployed.** Every change below sits in the working tree
for Brian to review and commit by hand.

## 1. Sessions — status

| Session | Status | Evidence |
|---|---|---|
| **S-08A.1a.2a** — discharge D-11 (spendVerified defaults to FALSE) | **ACCEPTED** | `evidence/S-08A.1a.2a-verify-2026-07-31_1848.log` (4/4, 893 tests) |
| **S-REACH-1** — reachability sweep | **ACCEPTED** (investigation, no code changed, no verify.ps1 gate applicable) | Findings in `DECISIONS.md`; no evidence log (nothing to verify) |
| **S-ACTIONS-LEGACY-1** *(new, discovered by S-REACH-1)* | NOT STARTED — new session assigned, not built this run | — |
| **S-AUTOLOG-1** — fix `automation_log`'s write path | **BUILT, NOT ACCEPTED — cold review BLOCK** | `evidence/S-AUTOLOG-1-final-verify-2026-07-31_2009.log` (4/4, 907 tests — the code itself is gate-clean; the BLOCK is a downstream-behavior finding a lint/test gate cannot catch) |
| **S-AUTOLOG-1.2** *(new, discovered by S-AUTOLOG-1's cold review)* | NOT STARTED — new session assigned, needs a fresh reviewer, not built this run | — |
| **S-SNAPSHOT-1** — fix `performance_snapshots` | **NOT STARTED** — queue stopped before this session per §7 | — |

No session was marked done while blocked. S-AUTOLOG-1's working-tree diff is real and mostly
correct (see §5) but is explicitly **not** to be treated as safe to deploy until S-AUTOLOG-1.2
lands and passes its own fresh cold review.

## 2. Test count per boundary; every test deleted or renamed

| Point | Count |
|---|---|
| Session start (harness floor at hand-off) | 888/888 |
| After S-08A.1a.2a (first pass) | 892/892 |
| After S-08A.1a.2a (cold-review corrections) | 893/893 |
| After S-AUTOLOG-1 (writer fixes + new tests) | 907/907 |
| After S-AUTOLOG-1 (cold-review revert of `table-expectations.js`) | 907/907 (net: -1 merge, +1 re-split to hold the floor — see below) |
| **Final** | **907/907** |

**Zero tests deleted.** Two renamed, both disclosed at the point of change (not silently):

- `tests/recommendation-score.test.js`: `'E1-PRESERVED: a zero-lead pure-waste term with
  derived (server-verified) cost still queues'` → `'E1 REGRESSES UNTIL S-07f.1 LANDS (D-11,
  re-affirmed 2026-07-31) — a zero-lead pure-waste term now gets ZERO credit, deliberately'`.
  Assertions changed from `spendClamped: false` / `score > 0` / queues, to `spendClamped: true`
  / `reclaimableSpend: 0` / `delta: 0` / does not queue — the disclosed, accepted D-11 cost.
- `tests/objective.test.js`: a test originally labeled `'ACCEPTANCE (REVIEW-D11 §3): ...'`
  was renamed to `'BOUND PROPERTY (A14 reproduced through the rows contract, W x honest): ...'`
  after cold review found it did not actually implement REVIEW-D11 §3's literal fixture. §3's
  literal fixture was added as its own, separately-labeled test, explicitly disclosed as
  **non-discriminating** (passes on both pre- and post-fix code, for reasons unrelated to this
  session's change — see §5).

**On the -1/+1 in S-AUTOLOG-1's second row:** mid-fix, two `table-expectations.test.js` tests
were briefly merged into one during a correction, dropping the count from 907 to 906 — caught
immediately because it would have violated the test floor (never falls). Re-split back to two
tests before final evidence was captured. Recorded here per R-006's own precedent: a test-count
fluctuation gets disclosed even when caught and corrected within the same session.

## 3. Reachability — every guard touched or added, and how confirmed

- **The W-multiple coherence cap (D-11).** Confirmed reachable **by mutation**: the fix was
  reverted via `git stash`, the new tests' exact numeric assertions were re-run against the
  pre-fix code, and all four failed (disproportionate candidate scored 10.6, unbounded, instead
  of clamping to the honest baseline of 2.2). The fix was then restored and the same tests
  re-run green. This was independently repeated by the cold reviewer, who performed the same
  mutation test rather than trusting the claim.
- **`automation_log`'s six writers.** Confirmed reachable by **real execution**, not by
  asserting a hand-maintained call-site list against a static string (the anti-pattern D-11's
  own cold review named): every new/updated test invokes the actual writer function
  (`acquireLockAndExecute`, `runAnalysisForAccount`, the `cron-*` handlers, `meta-creative.js`'s
  handler) and inspects the real row passed to `.insert()`. Two of the six writers
  (`meta-creative.js`, `cron-crm-sync.js`) had no test coverage at all before this session;
  minimal handler mocks were built specifically to reach this write path.
- **S-REACH-1's sweep** (5 pre-existing guards: `runBudgetGuardsForExecution`,
  `runBudgetGuardsForStaging`, `guardNegativeKeywordExecutionData`, `validateStatusPatch`,
  `canExecute`) — confirmed reachable by **call-graph trace**, file:line at each hop, from a
  real Vercel-routed handler (`api/actions.js`, `api/chat.js`, `api/execute-action.js`,
  `api/approve-action.js`) down to the guard. Full traces in `DECISIONS.md`'s S-REACH-1 entry.
- **The newly discovered coordinator gap (R-017).** Confirmed **not** merely alleged: read
  `autonomy-coordinator.js:157-232` directly and verified `getActiveCount`/`checkCap` filter
  `automation_log` on `account_id` + `status='complete'` + a time window only, with no
  `event_type` predicate anywhere, and that `checkPostureForAction` returns an absolute
  `verdict:'block'` when the cap is exceeded, called from live handlers `api/actions.js` and
  `api/chat.js`. Independently re-verified by me before accepting the cold reviewer's claim
  (SDR-2), not merely trusted.

## 4. `verify.ps1` result per session

| Session | Result | Evidence |
|---|---|---|
| S-08A.1a.2a (interim) | 4/4, 892 tests | `evidence/S-08A.1a.2a-verify-2026-07-31_1713.log` |
| S-08A.1a.2a (final, post cold-review corrections) | 4/4, 893 tests | `evidence/S-08A.1a.2a-verify-2026-07-31_1848.log` |
| S-AUTOLOG-1 (interim) | 4/4, 907 tests | `evidence/S-AUTOLOG-1-verify-2026-07-31_1959.log` |
| S-AUTOLOG-1 (final, post cold-review corrections) | 4/4, 907 tests | `evidence/S-AUTOLOG-1-final-verify-2026-07-31_2009.log` |

Additional `adhoc-verify-*.log` files in `evidence/` were produced by the cold-review subagents'
own independent `verify.ps1`/`npx vitest run` checks (they re-ran the suite themselves rather
than trusting this session's report) — left in place as their evidence, not this session's.

## 5. Cold review verdicts — quoted, with objections and resolutions

### S-08A.1a.2a — second attempt (first attempt stalled after 600s, no verdict, discarded)

> "VERDICT: APPROVE WITH NOTES. The fix is real, correctly scoped, and I verified it by
> execution and mutation testing rather than by reading the tests' claims..."

Two warn-level findings, both resolved in-session (label/prose corrections, not logic changes,
so no re-review was required):

1. A test labeled as REVIEW-D11 §3's acceptance test did not actually implement §3's literal
   fixture, and the reviewer found the literal fixture is **non-discriminating** (passes
   pre-and-post-fix, for an unrelated reason — a pre-existing terminal-vs-platformConversions
   coherence check, not this session's W-cap fix). **Resolved:** test renamed to state what it
   actually proves; §3's literal fixture added as its own, explicitly-disclosed-as-
   non-discriminating test.
2. The accepted-cost comments understated the regression as generic "under-crediting" when a
   zero-conversion cohort's reclaimable spend actually clamps to exactly $0 — full elimination
   of that class, not a reduction. **Resolved:** both comment blocks corrected.

One nit logged as new risk **R-016** (the `spendVerified` bypass has no structural enforcement
against a future one-line reopen), not fixed — assigned to S-07f.1's acceptance criteria.

### S-AUTOLOG-1 — BLOCK

> "VERDICT: BLOCK. One blocker. The audit-log fix is well-built and behaviorally neutral
> *inside* `execute-action-logic.js` — but `automation_log` is a **read** dependency of a
> terminal money-path gate, and making the writes succeed for the first time changes that
> gate's behavior on deploy. The session's own analysis does not mention this."

Full verdict, verbatim, is in `DECISIONS.md`'s S-AUTOLOG-1 entry. Summary:

1. **BLOCKER.** `autonomy-coordinator.js`'s `checkCap`/`getActiveCount` count every
   `status='complete'` `automation_log` row toward a terminal `block` verdict, with no
   `event_type` filter. This cap has never fired (0 rows, always). Deploying this session's fix
   makes writes succeed for the first time, so the cap could start firing on non-action noise
   (analysis runs, creative uploads, manual-gate approvals, guard deferrals) and silently freeze
   an account. **Not resolved this session** — assigned to new session **S-AUTOLOG-1.2**, per
   the queue's own instruction against reworking and re-reviewing in the same session.
2. **WARN.** Two `writeLog` call sites (manual-gate approval, budget-guard deferral) label a
   non-execution as `action_executed`. **Not resolved this session** — same reason, folded into
   S-AUTOLOG-1.2 (a real design decision, not a one-line patch).
3. **WARN.** `table-expectations.js`'s `automation_log` entry had been changed to
   `EXPECTED_EMPTY` mid-session, which silences the zero-row alert while production is, in
   fact, unchanged and still broken (nothing deployed). **Resolved in this session**: reverted
   to `EXPECTATION.BROKEN` (not merely downgraded to `EXPECTED_EMPTY_DISCLOSED`, which the
   reviewer suggested but which — checked directly against `evaluateTableExpectation`'s own
   logic — would have had the same alert-silencing problem). This correction was judged safe to
   apply without a fresh review: it is a monitoring-registry-only change, touches no execution
   logic or protected file, and narrows toward safety (restores an alert), not away from it.

Verified clean by the reviewer (independently, not taken on my account): the
`execute-action-logic.js` diff is entirely inside `writeLog` plus one import — lock acquisition,
budget guards, executor dispatch, `finalUpdate`, `recordActionOutcome` are byte-identical; the
`{ error }` checks are real on all six writers; the test-mock change in `execute-action.test.js`
didn't weaken any of ~40 pre-existing tests; the "validated live" claim investigation (§6 below)
is accurate; no seventh writer exists anywhere in the repo.

## 6. What the "validated live" claim actually rested on (S-AUTOLOG-1)

**Not `automation_log`** — confirmed 0 rows, always, since the table has existed.

Traced to `PRIME-TRIAGE-HANDOFF.md:43` (June 16, 2026): a human operator directly read the
budget value in the live Google Ads UI after a real mutation ($30→$31→$30 on campaign
`21613067659`), cross-referenced against the `actions` table's own `status`/`result` fields on
that specific row. This is a **credible, specifically-described, human-witnessed event** — not
a fabrication, and materially different from S-08A.1a's chat-surface fabrication incident where
a tool call was narrated but never ran.

**But no artifact this repo or a future auditor can independently check today exists**: no
screenshot was committed; whether the specific `actions` row from June 16 still exists could
not be verified (no `execute_sql`/query tool available, only schema introspection); and
`automation_log`, the table that would have made this durably auditable, has never recorded a
row for this or any event.

**Verdict, per the queue's own instruction:** the underlying event is credible; the CLAIM as
phrased in `PRIME-AGENCY-ROADMAP.md` ("validated live") and `MARKETING_AGENT_AUDIT.md`
("validated live in both directions") overstates what is durably re-checkable today, by
presenting a one-time, undocumented-by-artifact event as a standing fact. Recommended (not
actioned — those files are outside this run's file scope) downgrading both documents' phrasing
to name the actual artifact (`PRIME-TRIAGE-HANDOFF.md`) and disclose that no automated audit
trail exists for any execution until `automation_log` actually accrues rows post-deploy.

## 7. S-REACH-1's findings, with method

Full detail in `DECISIONS.md`'s S-REACH-1 entry. Summary — 5 confirmed REACHABLE (call-graph
traced, file:line at each hop, from a real Vercel-routed handler through to the guard); 1 new
finding, **S-ACTIONS-LEGACY-1**: `api/actions.js`'s legacy `POST {action:'execute', id}` branch
sets `status='executed'` on an `actions` row while bypassing every guard that transition is
supposed to go through (`canExecute`, `validateStatusPatch`, both budget guards, the
negative-keyword guard) — no platform mutation occurs, but downstream consumers would read a
fabricated "this happened" record. Predates Stage B1, disclosed there as a known, deliberate
carry-over ("no auth gap fixes" scope); no current UI client calls this shape (grepped
`marketing-bot-dashboard.jsx` directly). Not fixed — read-only investigation session, per its
own contract.

## 8. Discoveries given new session IDs rather than absorbed

- **S-ACTIONS-LEGACY-1** (S-REACH-1's finding) — the bypass-prone legacy execute branch.
- **S-AUTOLOG-1.2** (S-AUTOLOG-1's cold review) — the coordinator's missing `event_type` filter
  plus the two mislabeled non-execution writes.
- **R-016**, **R-017** — new risks logged in `DECISIONS.md`'s Risks table (spendVerified's lack
  of structural enforcement; the coordinator cap's lack of event_type scoping).

Nothing found was fixed inline beyond what is explicitly disclosed above as a safe,
non-money-path, alert-restoring correction.

## 9. Token usage

| Subagent | Tokens | Tool uses | Duration |
|---|---|---|---|
| Cold review, S-08A.1a.2a, attempt 1 (stalled, no verdict) | not reported (failed before completion) | — | ~600s+ (timed out) |
| Cold review, S-08A.1a.2a, attempt 2 | 74,994 | 23 | ~44 min |
| S-REACH-1 fork, attempt 1 (produced no real tool calls, discarded) | 268,179 | 0 | ~9s |
| S-REACH-1 fork, attempt 2 (real investigation) | not directly available to me — the completion notification for this run did not include a usage block | 28 (stated in its own report) | ~3 min (stated in its own report) |
| Cold review, S-AUTOLOG-1 | 67,687 | 30 | ~3.5 min |

**Main-thread usage is not directly observable to me in cumulative form** — stated plainly
rather than estimated, same limitation this repo's prior overnight report (2026-07-30) recorded.
If Brian needs an exact figure, Claude Code's own session/usage view is authoritative, not this
report.

## 10. Limitations and deviations — stated plainly

- **S-AUTOLOG-1's fix must NOT be deployed as-is.** It is real, mostly correct, and
  independently gate-clean (4/4, 907/907) — but deploying it without S-AUTOLOG-1.2 landing
  first would silently activate a dormant, unoverridable `block` gate on non-action noise. This
  is the entire reason the queue stopped here.
- **Two cold-review attempts stalled or produced no real work before succeeding** (one on
  S-08A.1a.2a, one on S-REACH-1). Both were discarded rather than counted as either an approval
  or a block, per the principle that an incomplete run is not a verdict. Relaunched fresh both
  times; the second attempt in each case did real, verifiable work.
- **The `actions` table's specific June-16 rows (the "validated live" claim's closest thing to
  an artifact) could not be queried** — this environment has schema introspection
  (`list_tables`) but no `execute_sql`/row-query tool. Stated as a limit, not guessed.
- **Whether R-017 is live-triggerable today depends on whether any `autonomy_posture` row has a
  non-null `cap_per_window`** — could not check, same tooling limit.
- **Two untracked, stray root files** (`PRIME-BUILD-QUEUE-V2.md`, `PRIME-COLD-REVIEW-D11.md`)
  duplicate/predate harness docs, verified by diff to be genuinely different documents (one is
  the reviewer's original prompt, not its output). Not deleted — same disposition as the prior
  R-007 finding: deletion is Brian's call. Flagged, not actioned.
- **Nothing committed. Nothing pushed. Nothing deployed.** Every file listed in this report sits
  in the working tree, unstaged, for Brian's review.

## What Brian needs to do

1. **Review the working tree by hand** — `git status`/`git diff` show everything; nothing is
   committed.
2. **Decide whether to fund S-AUTOLOG-1.2** before S-AUTOLOG-1's fix can ever be deployed — read
   `DECISIONS.md`'s S-AUTOLOG-1 entry and cold-review verdict in full first; it names the exact
   fix (an `event_type` filter plus a semantic fix to two mislabeled writes) but that fix needs
   its own build session and its own fresh cold reviewer, not this one.
3. **Decide whether to fund S-ACTIONS-LEGACY-1** (the audit-integrity bypass S-REACH-1 found) —
   not urgent (no live client calls it), but real.
4. **Decide on the two roadmap documents' "validated live" phrasing** — `PRIME-AGENCY-ROADMAP.md`
   and `MARKETING_AGENT_AUDIT.md` — per §6 above.
5. **Decide on the two stray root files.**
6. **No env vars needed.** No new paid services, no new credentials.
