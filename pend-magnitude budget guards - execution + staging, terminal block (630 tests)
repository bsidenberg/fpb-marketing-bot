[1mdiff --git a/api/lib/execute-action-logic.js b/api/lib/execute-action-logic.js[m
[1mindex da952a9..8b6d262 100644[m
[1m--- a/api/lib/execute-action-logic.js[m
[1m+++ b/api/lib/execute-action-logic.js[m
[36m@@ -37,6 +37,7 @@[m [mimport {[m
 } from './action-states.js';[m
 import { recordApiCall } from './api-cost.js';[m
 import { recordActionOutcome } from './autonomy-coordinator.js';[m
[32m+[m[32mimport { runBudgetGuardsForExecution } from './budget-guards.js';[m
 [m
 // ── Platform normaliser ───────────────────────────────────────────────────────[m
 export function normalizePlatform(platform) {[m
[36m@@ -612,7 +613,7 @@[m [mexport async function acquireLockAndExecute(actionId, { account, connection }) {[m
     .eq('id', actionId)[m
     .in('status', [STATUS.PENDING, STATUS.APPROVED])[m
     .is('result', null)[m
[31m-    .select('account_id, action_type, channel, execution_data')[m
[32m+[m[32m    .select('account_id, action_type, channel, execution_data, auto_execute')[m
     .single();[m
 [m
   // PGRST116 = no rows matched the WHERE (already locked or executed)[m
[36m@@ -634,6 +635,39 @@[m [mexport async function acquireLockAndExecute(actionId, { account, connection }) {[m
   let extraMeta      = {};[m
 [m
   try {[m
[32m+[m[32m    // ── Budget guards (SESSION-05) — spend magnitude & protection ─────────────[m
[32m+[m[32m    // 'block' is terminal: thrown into the shared error path, so the row is[m
[32m+[m[32m    // finalized with the guard reason and NO execution path — including a[m
[32m+[m[32m    // human approve click — may proceed. 'require_approval' refuses only[m
[32m+[m[32m    // auto-execution (auto_execute=true rows) and returns the action to the[m
[32m+[m[32m    // human queue; auto_execute=false rows only reach here via approve-action[m
[32m+[m[32m    // or the operator secret, which satisfies the approval requirement.[m
[32m+[m[32m    const guard = await runBudgetGuardsForExecution(locked, { account, connection });[m
[32m+[m[32m    if (guard.verdict === 'block') {[m
[32m+[m[32m      throw new Error(`Blocked by budget guard: ${guard.reason}`);[m
[32m+[m[32m    }[m
[32m+[m[32m    if (guard.verdict === 'require_approval' && locked.auto_execute === true) {[m
[32m+[m[32m      const { error: releaseErr } = await supabase[m
[32m+[m[32m        .from('actions')[m
[32m+[m[32m        .update({ result: null, auto_execute: false })[m
[32m+[m[32m        .eq('id', actionId);[m
[32m+[m[32m      if (releaseErr) {[m
[32m+[m[32m        console.error('[execute-action-logic] guard lock release failed:', releaseErr.code, releaseErr.message);[m
[32m+[m[32m      }[m
[32m+[m[32m      await writeLog({[m
[32m+[m[32m        actionId, accountId: account.id, now,[m
[32m+[m[32m        actionType,[m
[32m+[m[32m        platform:    supabasePlatform,[m
[32m+[m[32m        status:      'complete',[m
[32m+[m[32m        description: `${actionType} auto-execution deferred by budget guard — requires human approval: ${guard.reason}`,[m
[32m+[m[32m        metadata:    { budget_guard: 'require_approval', reason: guard.reason, ...(campaignId ? { campaign_id: campaignId } : {}) },[m
[32m+[m[32m      });[m
[32m+[m[32m      return {[m
[32m+[m[32m        httpStatus: 200,[m
[32m+[m[32m        body: { success: true, executed: false, requires_approval: true, reason: guard.reason },[m
[32m+[m[32m      };[m
[32m+[m[32m    }[m
[32m+[m
     if (actionType === 'publish_creative') {[m
       extraMeta = await executePublishCreative(locked, { account, connection });[m
     } else if (actionType === 'create_meta_campaign') {[m
[36m@@ -731,10 +765,28 @@[m [mexport async function executeTransient({ platform, actionType, campaignId }, { a[m
   // Synthetic action so executors have a uniform contract whether DB-backed or not[m
   const action = {[m
     action_type:    actionType,[m
[32m+[m[32m    channel:        normalizedPlatform, // budget guards fetch live Google state by channel[m
     execution_data: { campaign_id: campaignId },[m
   };[m
 [m
   const supabasePlatform = normalizedPlatform === 'google' ? 'google_ads' : 'meta_ads';[m
[32m+[m
[32m+[m[32m  // ── Budget guards (SESSION-05) — transient path enforces 'block' only ───────[m
[32m+[m[32m  // Transient executions are always human-initiated (chat confirmation click),[m
[32m+[m[32m  // so 'require_approval' is satisfied by the click; 'block' stays terminal.[m
[32m+[m[32m  const guard = await runBudgetGuardsForExecution(action, { account, connection });[m
[32m+[m[32m  if (guard.verdict === 'block') {[m
[32m+[m[32m    await writeLog({[m
[32m+[m[32m      actionId: null, accountId: account.id, now,[m
[32m+[m[32m      actionType,[m
[32m+[m[32m      platform:    supabasePlatform,[m
[32m+[m[32m      status:      'error',[m
[32m+[m[32m      description: `${actionType} blocked by budget guard (transient): ${guard.reason}`,[m
[32m+[m[32m      metadata:    { campaign_id: campaignId, transient: true, budget_guard: 'block', reason: guard.reason },[m
[32m+[m[32m    });[m
[32m+[m[32m    return { httpStatus: 200, body: { success: true, executed: false, blocked: true, error: `Blocked by budget guard: ${guard.reason}` } };[m
[32m+[m[32m  }[m
[32m+[m
   let executionError = null;[m
   let extraMeta      = {};[m
 [m
